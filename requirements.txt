pypsa==1.2.4
highspy==1.15.0
linopy==0.8.0
pandas==2.3.3
numpy==2.4.6
pyarrow==24.0.0
openpyxl==3.1.5
plotly==6.6.0
matplotlib==3.10.9
scipy==1.17.1
streamlit==1.57.0
pypsatopo==1.1.2
xarray==2026.2.0
netcdf4==1.7.4
geopandas==1.1.3
shapely==2.1.2
pyogrio==0.12.1
folium==0.20.0
streamlit-folium==0.27.2
# Typical-day clustering for the capacity-sizing LP (guarded import; the app
# runs without it). Requires highspy<=1.15.0 -- which is why highspy is pinned
# to 1.15.0 above rather than 1.15.1. 1.15.1 was only ever needed for the HiPO
# solver, which W15 benchmarked and rejected (dual simplex won), so nothing is
# lost. See docs/DEPLOYMENT.md.
tsam==3.4.2
