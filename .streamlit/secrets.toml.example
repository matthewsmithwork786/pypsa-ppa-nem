# No secrets are required to RUN the app -- all NEM data is committed under
# data/cache/ and read from disk.
#
# The acquisition scripts under scripts/ need this one, but they are run
# locally, never by the deployed app:
# OPENELECTRICITY_API_KEY = "..."
