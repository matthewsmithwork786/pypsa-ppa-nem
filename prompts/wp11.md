### WP11 — Documentation

**Owns:** `README.md`, `AGENTS.md`, `docs/DEPLOYMENT.md`, `docs/DATA_ACQUISITION.md`
(new), `docs/UAT_checklist.md`, `docs/PLAN_multiyear_sla.md`.

**Context — what is ACTUALLY true in this checkout right now** (the rest of the
multi-year/SLA feature has already been built and merged by other work
packages; document the real, current state, not aspirational plans):

- `Scenario` has `nem_years: tuple[int, ...]` (real field; `nem_year` is now a
  derived `@property` returning `nem_years[0]`), `capacity_sizing_year`,
  `nem_resolution_minutes` (5/15/30/60), `sla_monthly_enabled`,
  `sla_monthly_share`, `sla_daily_enabled`, `sla_daily_share`.
- `ppa/data/nem_data.py` gained `RUNTIME_CACHE_DIR` + `_resolve()` (packaged
  cache first, then `$PPA_RUNTIME_CACHE_DIR`, default
  `~/.cache/pypsa-ppa-nem/nem`), `load_plant_years`/`available_years`/
  `plants_operational_for_years` (reading `data/cache/nem/registry/plant_years.parquet`),
  `to_resolution`/`get_cf_dicts_multi`/`get_price_dict_multi`. It remains
  network-free (no `requests`/`urllib`/`httpx`/`nemosis`/`socket`/`streamlit`
  imports) — enforced by `tests/test_nem_data.py`.
- `ppa/data/remote_cache.py` is new: the **only** runtime module permitted
  network access (`ensure_plant_years`, `ensure_price_years`,
  `missing_plant_years`, `RemoteFetchError`). Fetches via HTTP range requests
  (fsspec+aiohttp, falls back to a full download when the server doesn't
  support ranges) so a 2-column read out of a wide per-year parquet transfers
  a small fraction of the file. `ppa/data/zenodo_manifest.json` currently has
  a **placeholder** `record_id` ("PLACEHOLDER_NOT_YET_PUBLISHED") — **no real
  Zenodo record has been published yet.** Publishing is a manual step Hanan
  owns (his own Zenodo account); until he does, `ensure_plant_years` will
  raise `RemoteFetchError` for any real fetch attempt, which the UI surfaces
  as a clean `st.error`, not a crash.
- `scripts/build_plant_year_manifest.py` is new and works today against the
  single committed 2025 year (it doesn't hardcode 2025 — it discovers whatever
  `<DUID>_<year>.parquet` files exist). It writes
  `data/cache/nem/registry/plant_years.parquet`. **This file does not exist in
  the committed repo cache yet** — running the script is a manual step, not
  wired into any automated pipeline. Document the gate rule in full (read the
  docstrings/constants at the top of the script — `MIN_COVERAGE=0.98`,
  the 3 Jan/29 Dec span, `MIN_MONTHLY_PEAK_RATIO=0.60` across all 12 months,
  `MAX_GAP_HOURS=MAX_ZERO_RUN_HOURS=336`, 60-day first-power lead time).
  **Important finding to document:** validating this script against the real
  2025 cache found that `LKBONNY2` — a plant the original design assumed
  would pass — actually fails the stricter all-12-months rule (its January
  2025 monthly peak was genuinely only 54% of its own annual peak, verified
  against raw data, not a bug). Record this as a known, explained outcome, not
  an open bug.
- `scripts/fetch_nem_availability.py`'s multi-year extension (`--year-start`/
  `--year-end`, checkpoint/resume, disk-safe streaming that deletes each
  month's raw AEMO archive immediately after extraction) and
  `scripts/build_zenodo_dataset.py` (the wide-per-year parquet builder for
  the real Zenodo upload) are **NOT YET BUILT** — deferred to a future work
  package, pending Hanan setting up a Zenodo account and this VM (or another
  machine) running a genuinely multi-day, network-heavy acquisition job. Note
  this clearly as a TODO in `docs/DATA_ACQUISITION.md`, don't imply it's done.
- The solver (`ppa/solver.py`) adds monthly/daily SLA constraints
  (`AllowedShortfall_Limit_M...`/`_D...`, `MinDelivery_Limit_M...`/`_D...`)
  alongside the existing annual one, correctly weighted by
  `n.snapshot_weightings["objective"]`. Monthly SLA is blocked under
  `sizing_method="tsam"` (calendar months don't exist on the clustered
  index) — both by a `validate_scenario` blocking error and a belt-and-braces
  `raise ValueError` in `solve()`. Daily SLA under tsam derives groups from
  the clusterer's `period_labels`, not the synthetic calendar.
- The UI: Pick Plants tab has a year-range slider, resolution selectbox,
  capacity-sizing-year selectbox (falls back to just `[2025]` with an
  explanatory caption while `plant_years.parquet` doesn't exist); Set Terms
  tab has a dedicated "Service level agreement (SLA)" expander (moved out of
  "PPA contract terms") with the four proactive warnings; Deep Dive tab has an
  SLA compliance section (monthly/daily delivery-share charts vs target).
- This VM: repo at `/home/matthewsmithwork786/pypsa-ppa-nem`, GCP instance
  `pypsa-ppa-dev`. Two Cloud Run services exist — `pypsa-ppa-nem-git`
  auto-deploys from `main` only; `pypsa-ppa-explorer` has no auto-trigger and
  is deployed manually via `gcloud builds submit --config cloudbuild.yaml`.
  Runtime cache dir defaults to `~/.cache/pypsa-ppa-nem/nem`; Streamlit Cloud/
  Cloud Run containers are ephemeral, so this refills on every cold start —
  document the expected first-fetch latency and that outbound HTTPS to
  `zenodo.org` is now needed (once a real record exists).

#### Specific edits

- `AGENTS.md` §2: add the invariant — *"`ppa/data/remote_cache.py` is the only
  runtime module permitted network access. `nem_data.py` must never import it;
  callers fetch first, then read."* (Verify this is not already stated near-
  identically by an earlier work package before duplicating it.)
- `AGENTS.md` §3: add a domain trap — *"tsam clustering destroys the calendar.
  Monthly SLA constraints are meaningless on clustered snapshots and are
  blocked in `validate_scenario` and again in `solve()`; daily constraints are
  applied per representative-day block via `period_labels` and are an
  approximation."*
- `AGENTS.md` §6 (repo-size note): the single-year availability cache may
  eventually be reduced or removed from the working tree in favour of Zenodo
  once a real record is published — not yet, since no such record exists.
  Deleting it later will not reclaim git history.
- `docs/DEPLOYMENT.md`: new section on the runtime cache per the bullet above.
- `docs/DATA_ACQUISITION.md` (new): the manifest-builder pipeline end to end
  (what's built, §2.2's rule in full, the LKBONNY2 finding), and a clearly
  labelled "Not yet built" section for the deferred fetch-script extension and
  Zenodo dataset builder, so a future session picks up exactly where this one
  left off.
- `docs/UAT_checklist.md`: add manual checks — pick a wind/solar plant over a
  year range, change resolution, toggle monthly/daily SLA and confirm the
  warnings/Deep Dive chart, confirm tsam+monthly is blocked with the
  explanatory message.
- `README.md`: brief mention of the multi-year/SLA capability if the README
  has a features list — keep it short, this file is user-facing.

**Done when:** every doc file accurately reflects the real current state (not
the original plan's aspirations), the full test suite is still green (docs
changes shouldn't break anything, but run it to be sure nothing else in your
Owns list — none of these are code — was accidentally left broken), and your
final report lists exactly what you wrote in each file.

---
Do not modify files outside the "Files owned" list above. If you believe you need
to, stop and report why.
